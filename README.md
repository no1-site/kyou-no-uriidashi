# 今日の売り出し — ショップ別価格比較

楽天市場で同一商品を確認したうえで、JANコードが取得できる商品はYahoo!ショッピングも横断して価格を比較し、GitHub Pagesで表示します。

## 更新

Windowsの `scripts/run-update-now.cmd` を実行します。保存済み認証情報を使い、最新コードの取得 → 楽天情報の取得 → Yahoo!ショッピング情報の取得 → Keepa情報の取得（後二者は設定時のみ）→ 公開前検証 → `products.json` の送信を行います。既存の毎日10:10のタスク登録をやり直す必要はありません。自動更新は、変更・ステージ済みファイルのない `main` ブランチでのみ実行します。

`[PRICE RESULT] compared=N reference_only=M` は楽天取得段階の結果です。統合後の最終結果は `collection_summary` を確認してください。`[PUBLICATION RESULT] published=N history=confirmed` がGitHubへの送信と履歴確定の成功を表します。GitHub Pagesへの反映完了は別途確認が必要です。

日次更新は公開用ファイルと価格履歴のコピーを `.local/update-*` に作って処理します。API取得・検証・送信が失敗した場合、更新前の `products.json` と履歴を維持します。履歴はGitHubへの送信成功後に確定します。送信成否不明、強制終了、送信成功後のローカル保存失敗の場合は復旧用データとロックを残し、次の自動更新を止めます。詳しくは [更新の保護と復旧](docs/update-safety.md) を参照してください。個別の `fetch-*.mjs` は診断用の低レベル処理で、この一括保護は `run-update-now.cmd` 経由の日次更新に適用されます。

## 実APIドライラン（公開しない確認）

Windowsで `scripts/run-update-dry-run.cmd` をダブルクリックします。現在のPCに保存済みの楽天・Yahoo!の暗号化認証情報を使い、楽天 → Yahoo!の実API取得と、日次更新と同じ公開前検証までを実行します。楽天・Yahoo!の認証が未設定なら失敗として終了します。ドライランではKeepaを必ずスキップし、保存済みキーがあっても読み込みません。

Gitの実行・commit・push・pullは行いません。公開用 `products.json` と本番履歴は読み取りだけで、処理と履歴のコピーは `.local/dry-run-*` 内に限定し、正常終了・通常の失敗時に削除します。APIキー・Client IDを表示／保存せず、取得スクリプトの標準出力・標準エラーも画面やログへ流しません。アフィリエイトIDは読み込みません。main以外の作業ブランチでも実行できます。

件数指定のない従来モードでは、終了時は次の7項目だけを日本語で表示します。ウィンドウを閉じるには何かキーを押してください。

1. 取得商品数：最終データの商品数（参考商品を含む）。
2. 比較成立商品数：2ショップ以上を照合でき、価格差による保留でない商品数。送料要確認の商品も含み、全件が安さのスコア対象とは限りません。
3. Yahoo追加商品数：Yahoo!出品を1店以上追加できた商品数。
4. Yahoo追加出品数：同一商品内のショップ重複をまとめた追加出品数。
5. Yahoo除外理由ごとの件数：各検索結果を最初に該当した理由で1回だけ計上。同一ショップの重複分も計上します。除外件数は商品数ではなく出品候補数です。
6. 保留商品数：統合後の価格差が3倍を超えるため判定を保留した商品数。
7. 実行時間：取得・検証・一時データ削除を含む秒数。

取得や検証に失敗した場合は、同じ7項目に「未完了」と安全な失敗区分だけを表示し、終了コード1を返します。日次更新と同じ `.local/update.lock` を使うため同時実行しません。強制終了でロックが残った場合は、プロセス終了を確認してから [復旧手順](docs/update-safety.md) に従ってください。ドライランのロックには `mode: dry-run` が記録され、本番履歴を確定する必要はありません。

## 100商品に向けた台帳・拡張ドライラン

通常運用は `config/tracking.json` の `productionMode: "legacy"` / `productionTarget: 12` に据え置きです。今回100商品は公開しません。台帳方式は件数指定のドライランで検証できます。

```bat
scripts\run-update-dry-run.cmd -TargetProductCount 20
scripts\run-update-dry-run.cmd -TargetProductCount 50
scripts\run-update-dry-run.cmd -TargetProductCount 100
```

一括試験は `scripts\run-update-dry-run.cmd -Sequence` で実行できます。20→65秒以上待機→50→65秒以上待機→100の順に進み、エラー時は後続を実行しません。個別に起動する場合も各試験間に65秒以上空けてください。同じアプリケーションIDを使う別PC・別作業フォルダーの更新とは同時実行しないでください。

必ず20→50→100の順に実行し、APIエラー・429・検証失敗・予算超過が出たら次へ進まないでください。目標未達だけでは照合条件を緩めません。Node直接実行では `TARGET_PRODUCT_COUNT` も指定できます。Windowsランチャーは保存済み暗号化認証だけを使用します。

拡張モードではリクエスト数・比較試行数・送料込み比較数・APIエラー・429・実行時間・JSONバイト数に加え、Yahooの実効平均間隔・実行時間・正規化したRetry-After秒数を表示します。429時の自動再試行は行いません。成功時の価格を含まない台帳案は `.local/catalog-proposal-N.json`、安全な集計結果は `.local/dry-run-result-N.json` に保存します。連続試験の集計と実測待機時間は `.local/dry-run-sequence-result.json` に保存します。取得途中の価格データは削除され、本番ファイル・履歴・認証・Gitは更新しません。

台帳、配分、更新予算、実API試験結果と本番切替前の確認事項は [100商品対応の運用設計](docs/catalog-scaling.md) を参照してください。**Yahooの間隔を2200msへ修正後、20→65秒待機→50→65秒待機→100の実API試験は全段階で成功しました。100目標では69商品で比較成立、569.8秒、429は0件です。100商品全件の定常運用は別途確認が必要で、本番件数は増やしていません。**

## 比較の方法と範囲

- 商品候補を調べる段階で、家電の交換電池・フィルター・ノズルなどの部品、ホビーの攻略本・主題歌CDなどの関連メディアを商品名から除外します。除外した候補は比較件数の上限に含めません。比較できない場合の参考商品にも同じ選定条件を使います。
- この選定は家電・ホビーを対象とし、洗剤の詰め替えなどは引き続き対象です。商品名の表記に依存するため、本体と付属品を一緒に記載した候補も除外される場合があります。
- 自動取得時の選定除外件数は `collection_summary.categories[].selection_excluded` に記録します。取得済みデータから掲載対象だけを見直した場合は `publication_review` を付け、元の取得日時・各店の価格は更新しません。

- 製品検索APIは製品名・JAN・メーカー・型番の取得だけに使用します。取得できなかった `averagePrice` や新品限定価格には依存しません。
- JAN、またはメーカーと型番で商品検索APIを検索します。検索にヒットしただけでは同一商品と判定しません。説明内のJANの明示、または商品名のメーカーと型番の一致を要求します。
- JANのチェックデジット、型番の末尾、容量・個数、中古・セット・選択式の表記、複数JANや価格のあるページを確認します。互換部品なども除外します。情報不足・曖昧な出品は除外するため、掲載件数が少ない場合があります。販売者の記載が正しいことまでは保証できません。
- 在庫あり・税込表示の出品だけを対象とし、同じショップの出品は最低の商品価格の1件にまとめます。2ショップ以上で比較成立です。同額でもショップ一覧を表示します。
- 「入数／50食」などの数量も照合します。「入数：50」や「●50●」など単位が確認できない出品は、単品・ケース販売のどちらかを推測せず除外します。
- 確認した最高価格が最低価格の3倍を超える場合は、その製品全体の比較を保留します。3倍は再確認のための運用上の基準で、誤価格の断定ではありません。高い店だけを削除して平均を作り直すことはしません。
- 従来モードは1カテゴリーあたり最大8製品を調べ、比較できた最大2製品を掲載します。台帳モードは設定された配分に従い、確認済みJANの追跡を先に行って不足枠だけ候補を調べます。商品検索は各検索の先頭30件です。楽天全体を網羅する比較ではありません。
- 送料判定は更新処理と画面で共通の `shipping-policy.mjs` を使います。商品検索APIの出力 `postageFlag` は0が送料込み、1が送料別です（入力の検索条件とは意味が異なります）。商品名に相反する表記、条件付き無料、送料見積もりがある場合は「送料要確認」にします。
- 送料込み表示の店が2店以上あれば、その店同士で平均との差・スコア・過去価格比較を表示します。カードの価格・リンク・ショップ名も、その比較対象の最安店に合わせます。送料別・未確認の店は参考欄に全件残し、平均・スコアに混ぜません。参考欄の方が支払総額では安い場合もあります。APIには配送先別の送料金額がないため、送料を推測して加算しません。送料込み表示も適用条件は販売ページで確認が必要です。
- 数量判定の改訂後は `validation_version: quantity-v2` の結果だけを価格比較として表示します。再取得前のデータは参考表示とし、平均価格・価格差・スコア・過去価格との比較を表示しません。過去価格の計算も同じ版で確認した履歴だけを対象にします。
- 送料込み表示で比較できる商品は、平均との差、ショップ数、レビュー、過去の記録価格からルールでスコアを計算します。アフィリエイト料率は順位に使いません。価格差は値下げ率や購入推奨を意味しません。

価格履歴はWindowsの `%LOCALAPPDATA%\KyouNoUriidashi\price-history.json` に保存します。その他の環境では `.local/price-history.json` です。旧方式の履歴とは別のキーを使い、同日の再実行を履歴の別日として数えません。
送料込み表示の店が2店以上の結果を履歴に追加し、`shipping_policy_version: postage-subset-v2` の履歴だけを過去価格比較に使います。記録価格は送料込み表示の比較対象内の最安価格です。送料別の安値を履歴に混ぜません。

## 診断

`products.json` の先頭要素に `collection_summary` を記録します。カテゴリーごとの検索件数・照合件数・除外理由が分かります。APIキー、リクエストURL、返却データ全体は記録しません。
`shipping_included_compared` が送料込み表示で判定できた商品数、`shipping_price_only` が送料の確認を要する商品数です。`compared` は両方を含むショップ照合済み件数です。日次更新では統合後に全体・カテゴリー別の集計を再計算し、価格差が3倍を超える保留商品は `comparison_held` に分けます。保留商品の平均・スコア・履歴比較は公開しません。取得済みデータを送料条件で再判定した場合は `shipping_review` に記録し、取得日時を変えません。

認証エラー・アクセス拒否・利用制限時は停止し、公開用ファイルは上書きしません。比較できないカテゴリーでは、安さを判定しない参考商品を表示します。

## 検証

Node.js 20以降で `node --test scripts/tests/*.test.mjs` を実行します。テストは実APIへ接続せず、平均価格がnullのケース、誤比較の除外、ショップの重複排除、障害時の保護、画面表示を確認します。Git送信のテストも一時ディレクトリ内のローカルリポジトリだけを使用します。GitはPATH、WindowsのGitHub Desktop、または `TEST_GIT_PATH` から利用します。実APIの取得成否はWindowsでの更新後に確認します。

## 公式仕様

送料金額の取得実験は `node scripts/probe-shipping.mjs` で実行できます。2店の公開配送案内から料金候補を抽出する調査用で、毎日の更新処理には組み込んでいません。商品の配送方法や例外との対応は未確定で、抽出金額を支払総額に加算しません。結果と制約は `docs/shipping-feasibility-2026-09-18.md` に記録しています。

- [楽天市場商品検索API](https://webservice.rakuten.co.jp/documentation/ichiba-item-search)
- [商品価格ナビ製品検索API](https://webservice.rakuten.co.jp/documentation/ichiba-product-search)
- [Yahoo!ショッピング アフィリエイトプログラム](https://developer.yahoo.co.jp/webapi/shopping/affiliate.html)


## Yahoo!ショッピング連携

Yahoo!デベロッパーネットワークでショッピングAPI用のClient IDを発行したら、Windowsで `scripts/setup-yahoo.ps1` を実行します。Client IDはWindowsユーザーに紐づく暗号化ファイルとして `%LOCALAPPDATA%\\KyouNoUriidashi\\yahoo-credentials.xml` に保存し、GitHubには保存しません。

連携後は、楽天側でJANコードまで確認できた掲載商品についてYahoo!ショッピングの商品検索（v3）をJANコードで検索します。在庫あり・新品・同一JANの商品だけを追加し、ストア単位で最低価格を採用します。Yahoo!ショッピングAPIの送料コード2（送料無料）は送料込み表示として扱い、コード1（設定なし）・3（条件付き送料無料）は送料要確認として平均・スコアから除外します。ポイント・クーポンは価格比較に未反映です。

Yahoo!の数量・中古・セット・選択式・関連部品・複数JANの判定は楽天と共通の照合関数を使います。商品名・説明・見出しに容量や個数の矛盾がある場合は除外します。基準商品に記載された容量・個数が確認できない場合、または基準の型番を商品名で完全一致確認できない場合も採用しません。条件が新品と明示されていない出品も除外します。照合済みJANと検証版を各出品に保存し、公開前に再確認します。保守的な判定のため、正しい商品でも店舗数が減る場合があります。

Yahoo!ショッピング商品検索v3はアプリケーションIDごとに1分30リクエストの制限があるため、応答後に最低2200ms空けて直列送信します。`YAHOO_REQUEST_INTERVAL_MS` は2200未満・非整数・非有限値を拒否します（未指定は2200）。Yahoo連携が未設定でも楽天のみの更新は継続します。

Yahoo!ショッピングのアフィリエイトは、ValueCommerceのYahoo!ショッピング用リファラルURL（またはURLエンコード済みの `affiliate_id`）を `scripts/setup-yahoo-affiliate.ps1` で保存すると有効になります。値はWindowsユーザーに紐づく暗号化ファイルへ保存し、GitHubには保存しません。設定後は商品検索APIへ `affiliate_type=vc` と `affiliate_id` を付け、APIが返す商品URLをアフィリエイトリンクとして利用します。未設定時は従来どおり通常の商品URLで価格比較します。


## Keepa / Amazon.co.jp連携

Keepa APIを設定すると、楽天側で確認したJAN/EANコードを使ってAmazon.co.jp（domain 5）の商品を照合します。Keepa公式APIはUPC・EAN・ISBN-13によるProduct Requestに対応しており、同一コードから複数ASINが返る場合があります。そのため、JAN一致を必須にし、型番と商品名の一致度も確認して候補を選びます。

Amazon価格はKeepaの `BUY_BOX_SHIPPING`（新商品のBuy Box価格・送料込み）を優先して比較対象にします。取得できない場合に `NEW` 価格があるときは送料条件未確認の参考値としてのみ掲載します。Keepa側の更新時刻や取得状況によりAmazonの商品・価格が取得できない場合があります。

Keepa APIキーは `scripts/setup-keepa.cmd` で設定します。キーはWindowsユーザーに紐づく暗号化ファイル `%LOCALAPPDATA%\\KyouNoUriidashi\\keepa-credentials.xml` に保存し、GitHubには保存しません。Keepa未設定時は従来どおり楽天市場＋Yahoo!ショッピングだけで更新します。

Keepa APIは有料サブスクリプション方式で、リクエストはトークンを消費します。現在の掲載数ではJANを最大100件まとめて照合する設計です。公開サービスでのデータ表示については、契約時に表示されるKeepaの最新利用条件も確認してください。
